新しい指標の計算ロジック

このコードは、ご提示の記事を参考に以下の計算を行っています。

    A率: 予測スコア1位の値。そのレースの最も信頼できる軸の信頼度。

    B率: 予測スコア1位と2位の合計値。上位2名で決まる信頼度。

    C率: 予測スコア1位、2位、3位の合計値。上位3名で決まる信頼度（3連複の期待値に近い）。

    D率: 予測スコア3位の値。3着以内に入るかどうかのボーダーライン選手の信頼度。

    CT値: (A率 - D率) / A率 で計算。1位と3位のスコア差を測る指標で、値が大きいほど**「1強」**のレースであることを示唆します。